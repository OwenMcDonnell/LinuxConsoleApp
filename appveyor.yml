-
  branches:
    only: 
      - test
  version: 1.0.{build}
  image: Visual Studio 2017

  environment:
    test_var: test
  build: 
   project: LinuxConsoleApp.sln
  install:
    - echo "in test branch"
  
-
  branches:
    only: 
      - master
  version: 1.0.{build}

  image: Visual Studio 2017
  #build: off
  environment:
    test_var: test
  install:
    - echo "in master branch"
